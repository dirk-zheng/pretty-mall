import { Link } from 'react-router-dom';
import { ArrowUpRight } from 'lucide-react';
import { productSlug } from '../data/seoContent';
import { categoryNames } from '../data/products';

export default function ProductCard({product,index=0}){return <article className="group"><Link to={`/products/${productSlug(product)}`}><div className="relative overflow-hidden rounded-[1.75rem] bg-[#eadfd8]"><img src={product.image} alt={product.name} className={`aspect-[4/5] w-full object-cover transition duration-700 group-hover:scale-105 ${index%3===1?'object-[55%_center]':index%3===2?'object-[80%_center]':'object-[24%_center]'}`} onError={e=>{e.currentTarget.src='/no-image.png'}}/><span className="absolute left-3 top-3 rounded-full bg-white/90 px-3 py-1.5 text-[10px] font-bold uppercase tracking-wider text-[#8d4940] backdrop-blur">{product.badge||categoryNames[product.category]}</span><span className="absolute bottom-3 right-3 grid h-11 w-11 place-items-center rounded-full bg-[#2d201d] text-white opacity-0 transition group-hover:opacity-100"><ArrowUpRight size={18}/></span></div><div className="mt-4"><p className="text-[11px] font-bold uppercase tracking-[.18em] text-[#a05247]">{categoryNames[product.category]}</p><div className="mt-1 flex items-start justify-between gap-3"><h2 className="font-heading text-xl font-medium text-[#2d201d]">{product.name}</h2><span className="font-semibold text-[#2d201d]">${product.price}</span></div><p className="mt-2 text-sm text-[#75635d]">{product.sizes} · {product.wash}</p></div></Link></article>}
